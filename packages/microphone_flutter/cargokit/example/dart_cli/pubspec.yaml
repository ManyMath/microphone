name: dart_cli_example
description: Example of a pure-Dart CLI package using cargokit for Rust FFI.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.5.0

dependencies:
  native_assets_cli: ^0.10.0
  build_tool:
    path: ../../build_tool
  ffi: ^2.1.0

dev_dependencies:
  test: ^1.24.0
